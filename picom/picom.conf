# Use the GLX backend for improved performance and compatibility
backend = "glx"

# Enable vertical synchronization to prevent screen tearing
vsync = true

# Disable shadows to maintain dwm's minimal aesthetic
shadow = false

# Define opacity rules for specific windows
opacity-rule = [
  "80:class_g = 'st'"  # Set st terminal to 80% opacity (20% transparency)
]
